# The only env vars backplane reads: how to reach the secret store.
# Everything else (HTTP addr, log level, sink providers, paths, ...)
# lives in code or in the secret store itself — never in env.

AURELION_SECRET_PROVIDER=file
AURELION_SECRETS_FILE=.secrets.json
